<% include partials/header %>

    <h1>Posts Page</h1>

    <form action="/addPost" method="POST">
        <input type="text" name="newpost" placeholder="Your Post">
        <input type="text" name="author" placeholder="Author">
        <button>Subimt</button>
    </form>

    <% posts.forEach((post) => { %>
        <li class="posts"><strong><%= post.title %></strong> - User ID: <%= post.author %></li>
    <% }) %>

    // <% for(let i = 0; i < posts.length; i++) { %>
    //     <li class="posts"><strong><%= posts[i].title %></strong> - User ID: <%= posts[i].author %></li>
    // <% } %>

    // <p>------</p>

    

<% include partials/footer %>